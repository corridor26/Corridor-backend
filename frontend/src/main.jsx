import React from 'react';
import ReactDOM from 'react-dom/client';
import CorridorApp from './CorridorComplete.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <CorridorApp />
  </React.StrictMode>
);
